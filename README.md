# py-line-notify

Simple Python Line Notify package

## Installation
```bash
pip install py-line-notify
```

## Usage
```python
import linenotify

token = 'YOUR_TOKEN'
linenotify.send(token, 'Hello Line Notify')
```
